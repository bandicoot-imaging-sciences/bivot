# Marks this directory as its own pnpm workspace root, stopping pnpm from
# walking up to ../../pnpm-workspace.yaml and hoisting installs into the
# parent bivot/ package's node_modules (which doesn't declare this
# package's dependencies). Without this, `pnpm install` here silently
# no-ops and requires `--ignore-workspace` to actually install.
